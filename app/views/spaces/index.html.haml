%p#notice= notice
%h1 Spaces
%table
  %thead
    %tr
      %th Name
      %th Org
      %th{:colspan => "3"}
  %tbody
    - @spaces.each do |space|
      %tr
        %td= space.name
        %td= space.org.name
        %td= link_to 'Show', space_path(space)
        %td= link_to 'Edit', url_for([:edit, space])
        %td= link_to 'Destroy', space_path(space, params: { org_id: space.org.id } ), method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Space', new_org_space_path
